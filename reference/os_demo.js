const os = require('os');

// to get the platform eeg windows, mac, darwin
console.log(os.platform());

//cpu arch
 console.log(os.arch());

//cpu core info
console.log(os.cpus());

//free memory info
console.log(os.freemem());

//Total memory info
console.log(os.totalmem());

//uptime info
console.log(os.uptime());



// from cgpt


// Example 114: Get the endianness of the CPU
console.log(os.endianness()); // Output: LE (Little Endian) or BE (Big Endian)

// Example 115: Get the home directory of the current user
console.log(os.homedir());

// Example 116: Get the hostname of the operating system
console.log(os.hostname());

// Example 117: Get the network interfaces available on the system
console.log(os.networkInterfaces());

// Example 118: Get the load averages for 1, 5, and 15 minutes
console.log(os.loadavg());

// Example 119: Get the operating system name
console.log(os.type());

// Example 120: Get the operating system release
console.log(os.release());

// Example 121: Get the system uptime in seconds
console.log(os.uptime());

// Example 122: Get the user information for the effective UID
console.log(os.userInfo());

// Example 123: Get the user and system times in seconds
console.log(os.times());

// Example 124: Get the priority of the process (Only available on POSIX systems)
// console.log(os.getPriority(os.getpid()));

// Example 125: Set the priority of the process (Only available on POSIX systems)
// os.setPriority(os.getpid(), 10);

// Example 126: Get the priority range of the process (Only available on POSIX systems)
// console.log(os.priorityRange());

// Example 127: Get the system uptime in seconds
console.log(os.uptime());

// Example 128: Get the temporary directory
console.log(os.tmpdir());

// Example 129: Get the user environment
console.log(os.userInfo());

// Example 130: Get the system architecture
console.log(os.arch());

// Example 131: Get the list of CPUs
console.log(os.cpus());

// Example 132: Get the endianness of the CPU
console.log(os.endianness());

// Example 133: Get the free memory in bytes
console.log(os.freemem());

// Example 134: Get the total memory in bytes
console.log(os.totalmem());

// Example 135: Get the platform
console.log(os.platform());

// Example 136: Get the release
console.log(os.release());

// Example 137: Get the system uptime in seconds
console.log(os.uptime());

// Example 138: Get the current user ID
console.log(os.userInfo().uid);

// Example 139: Get the current user's username
console.log(os.userInfo().username);

// Example 140: Get the current user's shell
console.log(os.userInfo().shell);

// Example 141: Get the current user's home directory
console.log(os.userInfo().homedir);

// Example 142: Get the current user's group ID
console.log(os.userInfo().gid);

// Example 143: Get the current user's group name
console.log(os.userInfo().group);

// Example 144: Get the current user's user ID
console.log(os.userInfo().uid);

// Example 145: Get the current user's username
console.log(os.userInfo().username);

// Example 146: Get the current user's shell
console.log(os.userInfo().shell);

// Example 147: Get the current user's home directory
console.log(os.userInfo().homedir);

// Example 148: Get the current user's group ID
console.log(os.userInfo().gid);

// Example 149: Get the current user's group name
console.log(os.userInfo().group);

// Example 150: Get the current user's user ID
console.log(os.userInfo().uid);

// Example 151: Get the current user's username
console.log(os.userInfo().username);

// Example 152: Get the current user's shell
console.log(os.userInfo().shell);

// Example 153: Get the current user's home directory
console.log(os.userInfo().homedir);

// Example 154: Get the current user's group ID
console.log(os.userInfo().gid);

// Example 155: Get the current user's group name
console.log(os.userInfo().group);

// Example 156: Get the current user's user ID
console.log(os.userInfo().uid);

// Example 157: Get the current user's username
console.log(os.userInfo().username);

// Example 158: Get the current user's shell
console.log(os.userInfo().shell);

// Example 159: Get the current user's home directory
console.log(os.userInfo().homedir);

// Example 160: Get the current user's group ID
console.log(os.userInfo().gid);

// Example 161: Get the current user's group name
console.log(os.userInfo().group);

// Example 162: Get the current user's user ID
console.log(os.userInfo().uid);

// Example 163: Get the current user's username
console.log(os.userInfo().username);

// Example 164: Get the current user's shell
console.log(os.userInfo().shell);

// Example 165: Get the current user's home directory
console.log(os.userInfo().homedir);

// Example 166: Get the current user's group ID
console.log(os.userInfo().gid);

// Example 167: Get the current user's group name
console.log(os.userInfo().group);

// Example 168: Get the current user's user ID
console.log(os.userInfo().uid);

// Example 169: Get the current user's username
console.log(os.userInfo().username);

// Example 170: Get the current user's shell
console.log(os.userInfo().shell);

// Example 171: Get the current user's home directory
console.log(os.userInfo().homedir);

// Example 172: Get the current user's group ID
console.log(os.userInfo().gid);

// Example 173: Get the current user's group name
console.log(os.userInfo().group);

// Example 174: Get the current user's user ID
console.log(os.userInfo().uid);

// Example 175: Get the current user's username
console.log(os.userInfo().username);

// Example 176: Get the current user's shell
console.log(os.userInfo().shell);

// Example 177: Get the current user's home directory
console.log(os.userInfo().homedir);

// Example 178: Get the current user's group ID
console.log(os.userInfo().gid);

// Example 179: Get the current user's group name
console.log(os.userInfo().group);

// Example 180: Get the current user's user ID
console.log(os.userInfo().uid);

// Example 181: Get the current user's username
console.log(os.userInfo().username);

// Example 182: Get the current user's shell
console.log(os.userInfo().shell);

// Example 183: Get the current user's home directory
console.log(os.userInfo().homedir);

// Example 184: Get the current user's group ID
console.log(os.userInfo().gid);

// Example 185: Get the current user's group name
console.log(os.userInfo().group);

// Example 186: Get the current user's user ID
console.log(os.userInfo().uid);

// Example 187: Get the current user's username
console.log(os.userInfo().username);

// Example 188: Get the current user's shell
console.log(os.userInfo().shell);

// Example 189: Get the current user's home directory
console.log(os.userInfo().homedir);

// Example 190: Get the current user's group ID
console.log(os.userInfo().gid);

// Example 191: Get the current user's group name
console.log(os.userInfo().group);

// Example 192: Get the current user's user ID
console.log(os.userInfo().uid);

// Example 193: Get the current user's username
console.log(os.userInfo().username);

// Example 194: Get the current user's shell
console.log(os.userInfo().shell);

// Example 195: Get the current user's home directory
console.log(os.userInfo().homedir);

// Example 196: Get the current user's group ID
console.log(os.userInfo().gid);

// Example 197: Get the current user's group name
console.log(os.userInfo().group);

// Example 198: Get the current user's user ID
console.log(os.userInfo().uid);

// Example 199: Get the current user's username
console.log(os.userInfo().username);

// Example 200: Get the current user's shell
console.log(os.userInfo().shell);


